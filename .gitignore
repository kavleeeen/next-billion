# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Secrets
.env

# Local database — the only store; rebuilt by `pipeline sync`.

data/*.db
data/*.db-journal

# macOS
.DS_Store
