# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Secrets
.env

# Local database — the only store; rebuilt by `pipeline sync`.

data/*.db
data/*.db-journal

# The viewer is committed. See docs/decisions/0014-the-viewer-ships.md

# macOS
.DS_Store
