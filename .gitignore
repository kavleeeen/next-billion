# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Secrets
.env

# Local database — the only store; rebuilt by `pipeline sync`.

data/*.db
data/*.db-journal

# Local-only web viewer. Not part of the deliverable; used to eyeball the data
# while building. The pipeline runs entirely from the CLI.
frontend/index.html

# macOS
.DS_Store
